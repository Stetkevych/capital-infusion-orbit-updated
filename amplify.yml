version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 18
        - cd client && npm ci
    build:
      commands:
        - cd client && npm run build
  artifacts:
    baseDirectory: client/build
    files:
      - '**/*'
  cache:
    paths:
      - client/node_modules/**/*
